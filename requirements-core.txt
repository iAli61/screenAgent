# ScreenAgent Requirements - Core Only
# Install only essential dependencies

# Core dependencies
numpy>=1.24.0
Pillow>=9.0.0

# Screenshot capture (choose one based on your system)
# For most systems (recommended):
pyautogui>=0.9.53

# Alternative for Linux with root access:
# mss>=6.1.0

# Optional dependencies - install separately if needed:
# python-dotenv>=0.19.0      # For .env file support
# keyboard>=0.13.5           # For keyboard shortcuts (Linux needs root)
# openai>=1.0.0             # For OpenAI integration
# azure-ai-inference        # For Azure AI (currently beta versions only)
